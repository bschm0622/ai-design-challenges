---
// Productivity App Landing Page - Premium Design
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexus — Where Genius Works</title>
    <meta name="description" content="The operating system for visionary minds. Nexus transforms how breakthrough ideas become reality.">
</head>
<body>
    <!-- Ambient Background -->
    <div class="ambient-bg">
        <div class="ambient-orb orb-1"></div>
        <div class="ambient-orb orb-2"></div>
        <div class="ambient-orb orb-3"></div>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <nav class="nav">
            <div class="nav-container">
                <div class="logo">
                    <span class="logo-mark">◊</span>
                    <span class="logo-text">Nexus</span>
                </div>
                <div class="nav-actions">
                    <span class="invitation">By invitation only</span>
                    <button class="btn-minimal">Request Access</button>
                </div>
            </div>
        </nav>

        <div class="hero-container">
            <div class="hero-content">
                <div class="hero-badge">
                    <span class="badge-dot"></span>
                    Used by visionaries at Apple, Stripe, and OpenAI
                </div>

                <h1 class="hero-title">
                    <span class="title-line">The operating system</span>
                    <span class="title-line">for <em class="title-emphasis">visionary</em> minds</span>
                </h1>

                <p class="hero-subtitle">
                    Nexus isn't another productivity tool. It's the cognitive infrastructure that amplifies human potential—transforming scattered thoughts into breakthrough innovations.
                </p>

                <div class="hero-stats">
                    <div class="stat">
                        <span class="stat-number">2.3×</span>
                        <span class="stat-label">Faster execution</span>
                    </div>
                    <div class="stat">
                        <span class="stat-number">94%</span>
                        <span class="stat-label">Idea retention</span>
                    </div>
                    <div class="stat">
                        <span class="stat-number">∞</span>
                        <span class="stat-label">Possibilities</span>
                    </div>
                </div>

                <div class="hero-cta">
                    <button class="btn-primary">
                        <span>Join the Waitlist</span>
                        <svg class="btn-arrow" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd" />
                        </svg>
                    </button>
                    <button class="btn-ghost">Watch the Vision</button>
                </div>
            </div>

            <div class="hero-visual">
                <div class="interface-container">
                    <div class="interface-glow"></div>
                    <div class="interface-screen">
                        <!-- Dynamic Content Layers -->
                        <div class="interface-layer layer-1">
                            <div class="floating-card card-idea">
                                <div class="card-header">💡 Breakthrough</div>
                                <div class="card-content">Revolutionary product concept</div>
                                <div class="card-connections">
                                    <div class="connection-line"></div>
                                    <div class="connection-line"></div>
                                </div>
                            </div>
                        </div>

                        <div class="interface-layer layer-2">
                            <div class="floating-card card-execution">
                                <div class="card-header">⚡ Execute</div>
                                <div class="progress-bar">
                                    <div class="progress-fill"></div>
                                </div>
                                <div class="card-meta">72% complete</div>
                            </div>
                        </div>

                        <div class="interface-layer layer-3">
                            <div class="floating-card card-insights">
                                <div class="card-header">📊 Insights</div>
                                <div class="insight-graph">
                                    <div class="graph-bar" style="height: 60%"></div>
                                    <div class="graph-bar" style="height: 80%"></div>
                                    <div class="graph-bar" style="height: 40%"></div>
                                    <div class="graph-bar" style="height: 100%"></div>
                                </div>
                            </div>
                        </div>

                        <div class="interface-neural-net">
                            <svg class="neural-svg" viewBox="0 0 400 300">
                                <defs>
                                    <filter id="glow">
                                        <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
                                        <feMerge>
                                            <feMergeNode in="coloredBlur"/>
                                            <feMergeNode in="SourceGraphic"/>
                                        </feMerge>
                                    </filter>
                                </defs>
                                <g class="neural-nodes">
                                    <circle class="neural-node" cx="50" cy="100" r="4"/>
                                    <circle class="neural-node" cx="150" cy="80" r="4"/>
                                    <circle class="neural-node" cx="250" cy="120" r="4"/>
                                    <circle class="neural-node" cx="350" cy="90" r="4"/>
                                    <circle class="neural-node" cx="100" cy="180" r="4"/>
                                    <circle class="neural-node" cx="200" cy="200" r="4"/>
                                    <circle class="neural-node" cx="300" cy="170" r="4"/>
                                </g>
                                <g class="neural-connections">
                                    <line class="neural-line" x1="50" y1="100" x2="150" y2="80"/>
                                    <line class="neural-line" x1="150" y1="80" x2="250" y2="120"/>
                                    <line class="neural-line" x1="250" y1="120" x2="350" y2="90"/>
                                    <line class="neural-line" x1="100" y1="180" x2="200" y2="200"/>
                                    <line class="neural-line" x1="200" y1="200" x2="300" y2="170"/>
                                </g>
                            </svg>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="features-container">
            <div class="section-header">
                <h2 class="section-title">Built for minds that change the world</h2>
                <p class="section-subtitle">Three pillars of cognitive amplification</p>
            </div>

            <div class="features-showcase">
                <div class="feature-main">
                    <div class="feature-visual">
                        <div class="thought-web">
                            <div class="thought-node active">
                                <span class="node-content">Core Idea</span>
                                <div class="node-pulse"></div>
                            </div>
                            <div class="thought-node">
                                <span class="node-content">Market Research</span>
                            </div>
                            <div class="thought-node">
                                <span class="node-content">Technical Specs</span>
                            </div>
                            <div class="thought-node">
                                <span class="node-content">User Stories</span>
                            </div>
                            <div class="thought-connection"></div>
                            <div class="thought-connection"></div>
                            <div class="thought-connection"></div>
                        </div>
                    </div>
                    <div class="feature-content">
                        <h3 class="feature-title">Neural Ideation Engine</h3>
                        <p class="feature-description">Your ideas don't exist in isolation. Nexus maps the invisible connections between concepts, revealing breakthrough opportunities that linear thinking misses.</p>
                        <div class="feature-metrics">
                            <span class="metric">40% more innovative solutions</span>
                        </div>
                    </div>
                </div>

                <div class="feature-main reverse">
                    <div class="feature-visual">
                        <div class="execution-pipeline">
                            <div class="pipeline-stage complete">
                                <div class="stage-icon">✓</div>
                                <span class="stage-label">Concept</span>
                            </div>
                            <div class="pipeline-stage active">
                                <div class="stage-icon">◐</div>
                                <span class="stage-label">Prototype</span>
                            </div>
                            <div class="pipeline-stage">
                                <div class="stage-icon">○</div>
                                <span class="stage-label">Launch</span>
                            </div>
                            <div class="pipeline-flow"></div>
                        </div>
                    </div>
                    <div class="feature-content">
                        <h3 class="feature-title">Quantum Execution</h3>
                        <p class="feature-description">Ideas without execution are just dreams. Our intelligent workflow orchestration turns visionary concepts into tangible outcomes with unprecedented speed.</p>
                        <div class="feature-metrics">
                            <span class="metric">300% faster time-to-market</span>
                        </div>
                    </div>
                </div>

                <div class="feature-main">
                    <div class="feature-visual">
                        <div class="collective-intelligence">
                            <div class="ci-center">
                                <div class="ci-avatar">You</div>
                            </div>
                            <div class="ci-orbit">
                                <div class="ci-collaborator">
                                    <div class="ci-avatar small">AI</div>
                                </div>
                                <div class="ci-collaborator">
                                    <div class="ci-avatar small">Team</div>
                                </div>
                                <div class="ci-collaborator">
                                    <div class="ci-avatar small">Data</div>
                                </div>
                                <div class="ci-collaborator">
                                    <div class="ci-avatar small">World</div>
                                </div>
                            </div>
                            <div class="ci-energy-rings">
                                <div class="energy-ring"></div>
                                <div class="energy-ring"></div>
                            </div>
                        </div>
                    </div>
                    <div class="feature-content">
                        <h3 class="feature-title">Collective Intelligence</h3>
                        <p class="feature-description">Tap into the wisdom of your network, AI insights, and real-time data. Make decisions backed by collective intelligence that spans human expertise and machine learning.</p>
                        <div class="feature-metrics">
                            <span class="metric">10× better decision accuracy</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Call to Action Section -->
    <section class="cta">
        <div class="cta-background">
            <div class="cta-grid"></div>
        </div>
        <div class="cta-container">
            <div class="cta-content">
                <h2 class="cta-title">Ready to amplify your genius?</h2>
                <p class="cta-subtitle">Join the cognitive revolution. Limited to 100 visionaries in our alpha program.</p>

                <div class="cta-form">
                    <input type="email" placeholder="Enter your email" class="cta-input">
                    <button class="cta-button">
                        <span>Request Invitation</span>
                        <div class="button-glow"></div>
                    </button>
                </div>

                <div class="cta-social-proof">
                    <div class="proof-avatars">
                        <div class="proof-avatar"></div>
                        <div class="proof-avatar"></div>
                        <div class="proof-avatar"></div>
                        <div class="proof-avatar more">+47</div>
                    </div>
                    <p class="proof-text">50 visionaries already in. 2,847 on waitlist.</p>
                </div>
            </div>
        </div>
    </section>
</body>
</html>

<style>
/* Font Loading */
@import url('https://fonts.googleapis.com/css2?family=Crimson+Pro:ital,wght@0,400;0,600;0,700;1,400&family=Inter:wght@300;400;500;600;700&display=swap');

/* CSS Reset & Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    line-height: 1.6;
    color: #0a0a0a;
    background-color: #000;
    overflow-x: hidden;
    font-weight: 400;
}

/* Typography */
h1, h2, h3 {
    font-family: 'Crimson Pro', Georgia, serif;
    font-weight: 600;
    line-height: 1.1;
    letter-spacing: -0.03em;
}

/* Ambient Background */
.ambient-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: -1;
}

.ambient-orb {
    position: absolute;
    border-radius: 50%;
    opacity: 0.4;
    filter: blur(60px);
    animation: float 20s ease-in-out infinite;
}

.orb-1 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(120, 119, 198, 0.3) 0%, transparent 70%);
    top: -200px;
    right: -200px;
    animation-delay: 0s;
}

.orb-2 {
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(255, 119, 198, 0.2) 0%, transparent 70%);
    bottom: -150px;
    left: -150px;
    animation-delay: -10s;
}

.orb-3 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(119, 198, 255, 0.15) 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation-delay: -5s;
}

@keyframes float {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    33% { transform: translate(30px, -30px) rotate(120deg); }
    66% { transform: translate(-20px, 20px) rotate(240deg); }
}

/* Navigation */
.nav {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 2rem 0;
}

.nav-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: white;
}

.logo-mark {
    font-size: 1.5rem;
    font-weight: 700;
    color: #7777c6;
}

.logo-text {
    font-family: 'Crimson Pro', serif;
    font-size: 1.25rem;
    font-weight: 600;
}

.nav-actions {
    display: flex;
    align-items: center;
    gap: 1.5rem;
}

.invitation {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 300;
}

.btn-minimal {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 400;
    cursor: pointer;
    transition: all 0.2s ease;
}

.btn-minimal:hover {
    border-color: rgba(255, 255, 255, 0.4);
    background: rgba(255, 255, 255, 0.05);
}

/* Hero Section */
.hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0 2rem;
    position: relative;
    background: radial-gradient(ellipse at top, rgba(17, 17, 17, 0.8) 0%, rgba(0, 0, 0, 1) 70%);
}

.hero-container {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
    width: 100%;
    padding: 4rem 0;
}

.hero-content {
    max-width: 600px;
}

.hero-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 2rem;
    font-weight: 400;
}

.badge-dot {
    width: 8px;
    height: 8px;
    background: #7777c6;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
    0%, 100% { opacity: 0.6; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.1); }
}

.hero-title {
    font-size: clamp(3.5rem, 7vw, 6rem);
    color: white;
    margin-bottom: 2rem;
    font-weight: 700;
    line-height: 0.95;
}

.title-line {
    display: block;
}

.title-emphasis {
    background: linear-gradient(135deg, #7777c6 0%, #ff77c6 50%, #77c6ff 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-style: italic;
}

.hero-subtitle {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 3rem;
    line-height: 1.7;
    font-weight: 400;
    max-width: 500px;
}

.hero-stats {
    display: flex;
    gap: 3rem;
    margin-bottom: 3rem;
}

.stat {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
}

.stat-number {
    font-size: 2rem;
    font-weight: 700;
    color: white;
    font-family: 'Inter', sans-serif;
}

.stat-label {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 400;
}

.hero-cta {
    display: flex;
    gap: 1rem;
    align-items: center;
}

/* Premium Buttons */
.btn-primary, .btn-ghost {
    padding: 0.875rem 1.5rem;
    border: none;
    border-radius: 8px;
    font-weight: 500;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    position: relative;
    overflow: hidden;
}

.btn-primary {
    background: linear-gradient(135deg, #7777c6 0%, #9966cc 100%);
    color: white;
    box-shadow: 0 4px 20px rgba(119, 119, 198, 0.3);
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(119, 119, 198, 0.4);
}

.btn-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: all 0.6s ease;
}

.btn-primary:hover::before {
    left: 100%;
}

.btn-arrow {
    width: 16px;
    height: 16px;
    transition: transform 0.3s ease;
}

.btn-primary:hover .btn-arrow {
    transform: translateX(2px);
}

.btn-ghost {
    background: transparent;
    color: rgba(255, 255, 255, 0.8);
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.btn-ghost:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 255, 255, 0.4);
    color: white;
}

/* Hero Visual */
.hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
}

.interface-container {
    width: 500px;
    height: 400px;
    position: relative;
    perspective: 1200px;
    transform: rotateY(-10deg) rotateX(5deg);
}

.interface-glow {
    position: absolute;
    inset: -20px;
    background: radial-gradient(ellipse, rgba(119, 119, 198, 0.3) 0%, transparent 70%);
    border-radius: 20px;
    filter: blur(30px);
    z-index: -1;
}

.interface-screen {
    width: 100%;
    height: 100%;
    background: rgba(17, 17, 17, 0.9);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    backdrop-filter: blur(20px);
    position: relative;
    overflow: hidden;
}

.interface-layer {
    position: absolute;
    z-index: 10;
}

.layer-1 {
    top: 40px;
    left: 40px;
    animation: float-card 6s ease-in-out infinite;
}

.layer-2 {
    top: 120px;
    right: 40px;
    animation: float-card 8s ease-in-out infinite;
    animation-delay: -2s;
}

.layer-3 {
    bottom: 40px;
    left: 60px;
    animation: float-card 7s ease-in-out infinite;
    animation-delay: -4s;
}

@keyframes float-card {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-10px) rotate(1deg); }
}

.floating-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 1rem;
    width: 180px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.card-header {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 0.5rem;
    font-weight: 500;
}

.card-content {
    font-size: 0.875rem;
    color: white;
    margin-bottom: 0.75rem;
}

.progress-bar {
    height: 4px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 2px;
    overflow: hidden;
    margin-bottom: 0.5rem;
}

.progress-fill {
    height: 100%;
    background: linear-gradient(90deg, #7777c6, #9966cc);
    width: 72%;
    animation: progress 3s ease-in-out infinite;
}

@keyframes progress {
    0%, 100% { width: 72%; }
    50% { width: 85%; }
}

.card-meta {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.5);
}

.insight-graph {
    display: flex;
    align-items: end;
    gap: 4px;
    height: 30px;
}

.graph-bar {
    width: 8px;
    background: linear-gradient(to top, #7777c6, #9966cc);
    border-radius: 2px;
    animation: graph 4s ease-in-out infinite;
}

@keyframes graph {
    0%, 100% { transform: scaleY(1); }
    50% { transform: scaleY(1.2); }
}

.interface-neural-net {
    position: absolute;
    bottom: 20px;
    right: 20px;
    width: 120px;
    height: 80px;
    opacity: 0.6;
}

.neural-svg {
    width: 100%;
    height: 100%;
}

.neural-node {
    fill: #7777c6;
    filter: url(#glow);
    animation: node-pulse 3s ease-in-out infinite;
}

.neural-line {
    stroke: rgba(119, 119, 198, 0.5);
    stroke-width: 1;
    animation: line-flow 2s linear infinite;
}

@keyframes node-pulse {
    0%, 100% { opacity: 0.6; r: 4; }
    50% { opacity: 1; r: 6; }
}

@keyframes line-flow {
    0% { stroke-dasharray: 0 10; }
    100% { stroke-dasharray: 10 0; }
}

/* Features Section */
.features {
    padding: 8rem 2rem;
    background: #111;
    position: relative;
}

.features-container {
    max-width: 1400px;
    margin: 0 auto;
}

.section-header {
    text-align: center;
    margin-bottom: 6rem;
}

.section-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    color: white;
    margin-bottom: 1rem;
    font-weight: 700;
}

.section-subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 400;
}

.features-showcase {
    display: flex;
    flex-direction: column;
    gap: 8rem;
}

.feature-main {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
}

.feature-main.reverse {
    direction: rtl;
}

.feature-main.reverse > * {
    direction: ltr;
}

.feature-visual {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 300px;
}

.feature-content {
    max-width: 500px;
}

.feature-title {
    font-size: 2.5rem;
    color: white;
    margin-bottom: 1.5rem;
    font-weight: 700;
}

.feature-description {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.7;
    margin-bottom: 2rem;
}

.feature-metrics {
    display: flex;
    gap: 1rem;
}

.metric {
    background: rgba(119, 119, 198, 0.1);
    border: 1px solid rgba(119, 119, 198, 0.3);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.875rem;
    color: #7777c6;
    font-weight: 500;
}

/* Feature Visuals */
.thought-web {
    position: relative;
    width: 300px;
    height: 200px;
}

.thought-node {
    position: absolute;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 0.75rem;
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.8);
    transition: all 0.3s ease;
}

.thought-node:nth-child(1) { top: 20px; left: 50px; }
.thought-node:nth-child(2) { top: 80px; right: 20px; }
.thought-node:nth-child(3) { bottom: 60px; left: 20px; }
.thought-node:nth-child(4) { bottom: 20px; right: 60px; }

.thought-node.active {
    background: rgba(119, 119, 198, 0.2);
    border-color: rgba(119, 119, 198, 0.5);
    color: white;
}

.node-pulse {
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    border: 2px solid #7777c6;
    border-radius: 10px;
    animation: pulse-ring 2s ease-out infinite;
}

@keyframes pulse-ring {
    0% { transform: scale(1); opacity: 1; }
    100% { transform: scale(1.3); opacity: 0; }
}

.execution-pipeline {
    display: flex;
    align-items: center;
    gap: 2rem;
    position: relative;
}

.pipeline-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
}

.stage-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    transition: all 0.3s ease;
}

.pipeline-stage.complete .stage-icon {
    background: #7777c6;
    color: white;
}

.pipeline-stage.active .stage-icon {
    background: rgba(119, 119, 198, 0.2);
    border: 2px solid #7777c6;
    color: #7777c6;
}

.pipeline-stage .stage-icon {
    background: rgba(255, 255, 255, 0.05);
    border: 2px solid rgba(255, 255, 255, 0.2);
    color: rgba(255, 255, 255, 0.5);
}

.stage-label {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.8);
}

.collective-intelligence {
    position: relative;
    width: 200px;
    height: 200px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.ci-center {
    z-index: 3;
}

.ci-avatar {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, #7777c6, #9966cc);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 600;
    font-size: 0.875rem;
}

.ci-avatar.small {
    width: 30px;
    height: 30px;
    font-size: 0.625rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.ci-orbit {
    position: absolute;
    inset: 0;
    animation: orbit 20s linear infinite;
}

.ci-collaborator {
    position: absolute;
}

.ci-collaborator:nth-child(1) { top: 0; left: 50%; transform: translateX(-50%); }
.ci-collaborator:nth-child(2) { right: 0; top: 50%; transform: translateY(-50%); }
.ci-collaborator:nth-child(3) { bottom: 0; left: 50%; transform: translateX(-50%); }
.ci-collaborator:nth-child(4) { left: 0; top: 50%; transform: translateY(-50%); }

@keyframes orbit {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

.ci-energy-rings {
    position: absolute;
    inset: -10px;
}

.energy-ring {
    position: absolute;
    inset: 0;
    border: 1px solid rgba(119, 119, 198, 0.3);
    border-radius: 50%;
    animation: energy-pulse 3s ease-in-out infinite;
}

.energy-ring:nth-child(2) {
    inset: -20px;
    animation-delay: -1.5s;
}

@keyframes energy-pulse {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 0.8; transform: scale(1.1); }
}

/* CTA Section */
.cta {
    padding: 8rem 2rem;
    position: relative;
    background: #000;
}

.cta-background {
    position: absolute;
    inset: 0;
    opacity: 0.1;
}

.cta-grid {
    width: 100%;
    height: 100%;
    background-image:
        radial-gradient(circle at 1px 1px, rgba(255,255,255,0.15) 1px, transparent 0);
    background-size: 50px 50px;
}

.cta-container {
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

.cta-content {
    text-align: center;
}

.cta-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    color: white;
    margin-bottom: 1.5rem;
    font-weight: 700;
}

.cta-subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 3rem;
    line-height: 1.6;
}

.cta-form {
    display: flex;
    gap: 1rem;
    justify-content: center;
    align-items: center;
    margin-bottom: 2rem;
    flex-wrap: wrap;
}

.cta-input {
    padding: 1rem 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    color: white;
    font-size: 1rem;
    min-width: 300px;
    transition: all 0.3s ease;
}

.cta-input::placeholder {
    color: rgba(255, 255, 255, 0.5);
}

.cta-input:focus {
    outline: none;
    border-color: #7777c6;
    box-shadow: 0 0 0 3px rgba(119, 119, 198, 0.2);
}

.cta-button {
    padding: 1rem 2rem;
    background: linear-gradient(135deg, #7777c6 0%, #9966cc 100%);
    border: none;
    border-radius: 8px;
    color: white;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
}

.cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(119, 119, 198, 0.4);
}

.button-glow {
    position: absolute;
    inset: -2px;
    background: linear-gradient(135deg, #7777c6, #9966cc);
    border-radius: 10px;
    z-index: -1;
    filter: blur(8px);
    opacity: 0;
    transition: opacity 0.3s ease;
}

.cta-button:hover .button-glow {
    opacity: 0.7;
}

.cta-social-proof {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
}

.proof-avatars {
    display: flex;
    gap: -0.5rem;
}

.proof-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: linear-gradient(135deg, #7777c6, #9966cc);
    border: 2px solid #000;
    margin-left: -8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.625rem;
    color: white;
    font-weight: 600;
}

.proof-avatar:first-child {
    margin-left: 0;
}

.proof-avatar.more {
    background: rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.8);
}

.proof-text {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.875rem;
}

/* Responsive Design */
@media (max-width: 1024px) {
    .hero-container {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 4rem;
    }

    .feature-main {
        grid-template-columns: 1fr;
        gap: 4rem;
        text-align: center;
    }

    .feature-main.reverse {
        direction: ltr;
    }

    .hero-stats {
        justify-content: center;
    }

    .interface-container {
        transform: none;
    }
}

@media (max-width: 768px) {
    .hero {
        padding: 0 1rem;
    }

    .features, .cta {
        padding: 6rem 1rem;
    }

    .nav-container {
        padding: 0 1rem;
    }

    .nav-actions {
        flex-direction: column;
        gap: 0.5rem;
    }

    .hero-cta {
        flex-direction: column;
        width: 100%;
    }

    .hero-cta .btn-primary,
    .hero-cta .btn-ghost {
        width: 100%;
        max-width: 300px;
    }

    .cta-form {
        flex-direction: column;
        align-items: stretch;
    }

    .cta-input {
        min-width: auto;
        width: 100%;
    }

    .hero-stats {
        gap: 2rem;
    }
}

@media (max-width: 480px) {
    .features-showcase {
        gap: 6rem;
    }

    .proof-avatars {
        gap: 0;
    }

    .interface-container {
        width: 350px;
        height: 280px;
    }

    .floating-card {
        width: 140px;
        padding: 0.75rem;
    }
}
</style>